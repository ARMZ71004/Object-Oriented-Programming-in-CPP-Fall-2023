/*

// merge two sublists into a masterlist in sorted order
void merge(subList1, subList2, masterList) 
{
    // point iterator to first node of sublist1                     
    // point iterator to first node of sublist2 

    // while both lists have more data  ( copy data in sorted order from subList1 and subList2 to the masterList)
    //      if the target value in subList1 < subList2 ( the target value is what we are sorting )
    //          add the subList1 value to the masterList
    //          advance the iterator for subList1
    //      otherwise
    //          add the subList2 value to the masterList
    //          advance the iterator for subList2  

    // while subList1 has more data ( copy leftover data from subList1 to the masterList)
    //      add the subList1 value to the masterList
    //      advance the iterator for subList1
    // while subList2 has more data (copy leftover data from subList2 to the masterList)
    //      add the subList2 value to the masterList
    //      advance the iterator for subList2           
}

// recursively separate a list into two sublists until each sublist has one node 
void mergeSort(masterList) 
{
    // get the size of the masterList
    // if the masterList is empty return

    // create two sublists, subList1, subList2
    // point an iterator to the first node in the masterList

    // iterate through the first half of the masterList
    //      add value from the masterList to subList1
    //      advance the iteratfor for masterList

    // iterate through the second half of the masterList
    //      add value from the masterList to subList2
    //      advance the iteratfor for masterList    

    // remove all values from the masterList

    // recurse on subList1
    // recurse on subLIst2
    // merge subList1, subList2 and the masterList
}

*/